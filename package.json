{
    "name": "promptsonar",
    "private": true,
    "workspaces": [
        "packages/*",
        "action"
    ],
    "scripts": {
        "build": "npm run build --workspace packages/core && npm run build --workspace packages/cli && npm run build --workspace packages/cursor-extension && npm run build --workspace packages/claude-code && npm run build --workspace packages/dashboard && npm run build --workspace action",
        "test": "npm run build --workspace packages/core && npm run test --workspace packages/core && npm run test --workspace packages/cli && npm run test --workspace packages/cursor-extension && npm run test --workspace packages/claude-code && npm run test --workspace packages/vscode-extension && npm run test --workspace action",
        "lint": "eslint 'packages/*/src/**/*.ts' 'action/src/**/*.ts'",
        "clean": "rimraf packages/*/dist action/dist",
        "adoption:metrics": "node scripts/capture-adoption-metrics.js",
        "release:hygiene": "node scripts/check-release-hygiene.js",
        "smoke:features": "npm run build --workspace packages/core && npm run build --workspace packages/cli && node scripts/run-feature-smoke.js",
        "benchmark:mcp": "npm run build --workspace packages/core && npm run build --workspace packages/cli && node scripts/run-mcp-benchmark.js"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "rimraf": "^5.0.0",
        "eslint": "^8.57.0",
        "@typescript-eslint/parser": "^7.0.0",
        "@typescript-eslint/eslint-plugin": "^7.0.0"
    },
    "overrides": {
        "next@15.5.18": {
            "postcss": "8.5.15"
        },
        "postcss": "8.5.15"
    },
    "version": "1.4.3"
}
