{
  "name": "dashboard",
  "version": "1.5.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "predev": "npm --prefix ../core run build && npm --prefix ../cli run build",
    "dev": "next dev",
    "build": "npm --prefix ../core run build && npm --prefix ../cli run build && next build",
    "start": "next start",
    "lint": "eslint src"
  },
  "dependencies": {
    "@promptsonar/core": "*",
    "@supabase/supabase-js": "^2.106.0",
    "next": "15.5.18",
    "react": "^18",
    "react-dom": "^18",
    "stripe": "^22.1.1",
    "@promptsonar/cli": "*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
