"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.evaluatePrompt = evaluatePrompt;
const vague_words_1 = require("./clarity/vague_words");
const owasp_patterns_1 = require("./security/owasp_patterns");
const pii_1 = require("./security/pii");
const token_limit_1 = require("./efficiency/token_limit");
const json_mode_1 = require("./structure/json_mode");
const contradictions_1 = require("./consistency/contradictions");
__exportStar(require("./types"), exports);
function evaluatePrompt(input, config = {}) {
    const findings = [
        ...(0, vague_words_1.checkVagueWords)(input),
        ...(0, owasp_patterns_1.checkOwaspPatterns)(input),
        ...(0, pii_1.checkPii)(input),
        ...(0, token_limit_1.checkTokenLimit)(input, config?.efficiency?.token_budget || 8192),
        ...(0, json_mode_1.checkJsonMode)(input),
        ...(0, contradictions_1.checkContradictions)(input)
    ];
    // Scoring weights: clarity: 25%, security: 30%, efficiency: 20%, structure: 15%, consistency: 10%
    // Instead of dynamic weights per finding, we will sum the penalty_scores and deduct from 100.
    // The system prompt logic: "deterministic weighted sum ONLY (no LLM randomness): clarity: 25%, security: 30%, ... Final score = 100 - sum(penalties), clamped 0-100"
    let totalPenalty = 0;
    for (const finding of findings) {
        if (finding.category === 'clarity')
            totalPenalty += (finding.penalty_score || 0) * 0.25;
        if (finding.category === 'security')
            totalPenalty += (finding.penalty_score || 0) * 0.30;
        if (finding.category === 'efficiency')
            totalPenalty += (finding.penalty_score || 0) * 0.20;
        if (finding.category === 'structure')
            totalPenalty += (finding.penalty_score || 0) * 0.15;
        if (finding.category === 'consistency')
            totalPenalty += (finding.penalty_score || 0) * 0.10;
    }
    // Wait, if it's "100 - sum(penalties)", maybe penalties are raw points and the categories define max points?
    // Let's just deduct raw penalty sums since rules define their own penalty.
    // We'll scale them by weights to honor the "weighted sum" requirement.
    let status = "pass";
    let score = Math.max(0, Math.min(100, Math.round(100 - totalPenalty)));
    if (score < 70) {
        status = "fail";
    }
    else if (score < 85) {
        status = "warn";
    }
    const hasCritical = findings.some(f => f.severity === 'critical');
    if (hasCritical) {
        score = Math.min(score, 49); // Hard fail ceiling
        status = "fail";
    }
    // Strip internal fields like penalty_score from output
    const cleanFindings = findings.map(f => {
        const { penalty_score, ...rest } = f;
        return rest;
    });
    return {
        score,
        status,
        findings: cleanFindings
    };
}
