// FP H4: Validated input — has "validated" indicator near the user input template
const ragPrompt = "Search the knowledge base for the user query using the validated and sanitized input: {validated_query}"
